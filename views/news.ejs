<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>MUSCLER</title>
    <link rel="stylesheet" type="text/css" href="/public/css/style.css">
</head>
<body>
    <%- include('header'); %>
    <main>
        <p class="home-title">ニュース</p>
        <a class="btn btn-home" href="/create">＋投稿する</a>
        <ul class="list">
          <% articles.forEach((article) => { %>
          <li>
            <h2><%= article.title %></h2>
            <p><%= article.summary %></p>
            <a href="/article/<%= article.id %>">続きを読む</a>
          </li>
          <% }) %>
        </ul>
    </main>
    <%- include('footer'); %> 
</body>
</html>